<div class="login-session">
  <div class="login-session-error">
    <%= devise_error_messages! %>
  </div>
  <div class="login-session-container">
    <h2 style="font-size:4vh;">Sign up</h2>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <div class="field">
        <%= f.label :email %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>
      <div class="field" >
        <div style=" font-size: 2vh;width:100%;">
        <%= f.label :password %>
        <% if @minimum_password_length %>
        <em style="font-size: 1.5vh;">(<%= @minimum_password_length %> characters minimum)</em>
        <% end %>
        </div>
        <%= f.password_field :password, autocomplete: "off" %>
      </div>
      <div class="field">
        <%= f.label :password_confirmation %>
        <%= f.password_field :password_confirmation, autocomplete: "off" %>
      </div>
      <div class="field">
        <%= f.label :name %>
        <%= f.text_field :name, autocomplete: "off" %>
      </div>
      <div class="field">
        <%= f.label :age %>
        <%= f.text_field :age %>
      </div>
      <div class="field">
        <%= f.label :sex %>
        <%= f.text_field :sex %>
      </div>
      <div class="field">
        <%= f.label :avatar do %>
          <%= f.file_field :avatar %>
          <%= f.hidden_field :avatar_cache %>
        <% end %>
      </div>

        <%= f.submit "Submit!" %>
    <% end %>
  </div>
  <div class="login-session-forget">
    <%= render "devise/shared/links" %>
  </div>
  <div class="login-session-explain">
      <h1>[ 성 교 ]</h1>
      <h2>KU UNIVERSITY</h2>
  </div>
</div>

<script>
   $(".login-session-container").css({"width":"100vh","height":"100vh"});
   $(".login-session").css({"min-height":"120%"});
    $(".field").css({"width":"40vh"});
</script>